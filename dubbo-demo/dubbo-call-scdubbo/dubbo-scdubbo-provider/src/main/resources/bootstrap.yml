server:
  port: 8099

spring:
  application:
    name: scdubbo-provider
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: true

management:
  security:
    enabled: false

  endpoint:
    consul:
      enabled: false
  health:
    consul:
      enabled: false

endpoints:
  consul:
    enabled: false
